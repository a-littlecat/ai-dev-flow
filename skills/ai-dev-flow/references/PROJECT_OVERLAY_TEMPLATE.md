# 项目适配模板

> 使用方式：复制到目标项目的 `docs/PROJECT_OVERLAY.md` 或合并进 `AGENTS.md`。本模板只提供占位符，不包含任何具体项目业务规则。

## 项目名称

`<项目名称>`

## 项目目标

- `<项目要解决的问题>`
- `<主要用户或使用场景>`

## 技术栈

- 语言：`<语言>`
- 框架：`<框架>`
- 运行环境：`<运行环境>`
- 关键依赖：`<关键依赖>`

## 主要目录

| 目录 | 说明 |
| --- | --- |
| `<path>` | `<说明>` |

## 构建命令

```text
<构建命令>
```

## 测试命令

```text
<测试命令>
```

## 运行命令

```text
<运行命令>
```

## 禁止修改区域

- `<不得修改的目录或文件>`
- `<需要用户确认后才能修改的范围>`

## 重点审查项

- `<安全风险>`
- `<兼容性风险>`
- `<性能风险>`
- `<用户体验风险>`

## 项目专用风险

- `<风险 1>`
- `<风险 2>`

## 发布方式

- 发布触发：`<手动 / 自动 / 待确认>`
- 发布命令：`<命令或不适用>`
- 发布前检查：`<检查项>`

## 用户动作等级 / 验收方式

1. `<验收步骤 1>`
2. `<验收步骤 2>`
3. `<验收步骤 3>`

## 适用的任务等级说明

| 等级 | 本项目中的含义 |
| --- | --- |
| A | `<文档或低风险小任务示例>` |
| B | `<小代码任务示例>` |
| C | `<中等任务示例>` |
| D | `<高风险任务示例>` |

## Batch / Wave 适配

- A 级任务批量上限：`<例如 3~8 / 待确认>`
- B 级任务批量上限：`<例如 2~3 / 待确认>`
- 允许并行的目录或模块：`<path 或模块名>`
- 默认独占锁文件：`<入口文件 / 构建配置 / 协议文件 / 待确认>`
- 禁止并行的模块：`<核心模块 / 数据模块 / 发布模块 / 待确认>`
- 是否允许 agent 启动 Parallel Wave：`<必须用户确认 / 项目规则授权 / 禁止>`
