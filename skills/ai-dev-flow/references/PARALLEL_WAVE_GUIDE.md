# Parallel Wave 并行波次指南

Parallel Wave 用于让多个执行会话同时处理多个互不冲突的任务。Wave 是调度方式，不改变任务责任边界。

TASK 是最小责任单位；Wave 不能替代单个 TASK 的记录、审查和验收。

## 核心概念

### Single Task

一个执行会话只执行一个任务。

适合：

- C/D 任务。
- 高风险任务。
- 任务边界不清的任务。
- 涉及核心模块、真实环境、实机测试、回归验收或用户决策的任务。

### Batch

一个执行会话顺序执行多个 A/B 小任务。

适合：

- 多个低风险文档任务。
- 多个低风险模板任务。
- 少量互不冲突的小代码任务。

### Parallel Wave

多个执行会话同时执行多个互不冲突的任务。

适合：

- 文件范围明确。
- 模块边界清楚。
- 任务之间无依赖。
- diff 可拆分。
- 可以分别审查和回滚的任务。

## 可以进入 Parallel Wave 的任务

### A 级文档任务

可以并行。

条件：

- 文件范围明确。
- 不同时修改同一个核心文档。
- 不影响业务代码。
- 不需要实机测试。

### B 级小代码任务

可以谨慎并行。

条件：

- 修改文件不重叠。
- 不涉及核心模块。
- 不涉及构建、发布、依赖。
- 不涉及通信协议。
- 不涉及数据写入、删除、迁移。
- 可独立验证。
- 可独立回滚。

### C 级中等任务

默认不推荐并行，但允许和 A 级文档任务并行。

如需与其他代码任务并行，必须满足：

- 独立 Worktree。
- 模块不重叠。
- 文件不重叠。
- 依赖关系清楚。
- 用户明确确认。

### D 级高风险任务

默认不允许进入代码并行。

D 级任务只能和只读分析、文档整理、审查任务并行，不得和其他代码任务同时执行。

## 禁止并行

以下情况必须串行：

- 两个任务修改同一文件。
- 两个任务修改同一核心模块。
- 两个任务修改同一接口、协议、DTO、schema。
- 一个任务依赖另一个任务结果。
- 任务涉及架构迁移。
- 任务涉及技术栈迁移。
- 任务涉及构建 / 发布 / 依赖。
- 任务涉及核心服务。
- 任务涉及进程间通信 / 通信协议。
- 任务涉及项目入口或 UI host 启动链路。
- 任务涉及前端全局状态流。
- 任务涉及数据写入、删除、迁移。
- 任务涉及真实软件插件或真实环境。
- 任务需要 UA5 / UA6 / UA7。
- 当前工作区已有来源不明的未提交改动。
- agent 无法判断冲突风险。

## 文件锁 / 模块锁

每个并行任务开始前必须声明：

- 预计修改文件。
- 预计读取文件。
- 预计影响模块。
- 禁止修改文件。
- 禁止修改模块。

如果两个任务的“预计修改文件”重叠，不得并行。

如果两个任务的“预计影响模块”重叠，默认不得并行，除非用户明确确认。

核心文件默认需要独占锁，例如：

- 项目入口文件。
- 构建配置。
- 全局状态文件。
- 协议 / DTO / schema。
- 核心服务。
- 主流程编排文件。

## Wave 文件格式

可选创建 `docs/waves/WAVE-xxx.md`：

```markdown
# WAVE-xxx：并行波次标题

## 波次目标

- 待填写

## 波次类型

- 文档并行 / 低风险代码并行 / 混合并行 / 审查并行

## 包含任务

| 任务编号 | 任务名称 | 等级 | 执行会话 | 执行位置 | 是否可并行 | 原因 |
| --- | --- | --- | --- | --- | --- | --- |
| TASK-001 | 待填写 | A / B / C / D | 会话 A | 主项目 / 分支 / Worktree | 是 / 否 | 待填写 |

## 并行准入检查

- 是否存在文件冲突：
- 是否存在模块冲突：
- 是否存在依赖关系：
- 是否涉及核心模块：
- 是否涉及 UA5 / UA6 / UA7：
- 是否需要用户确认：

## 文件锁 / 模块锁

| 任务编号 | 预计修改文件 | 预计影响模块 | 锁类型 |
| --- | --- | --- | --- |
| TASK-001 | 待填写 | 待填写 | 独占 / 可共享 |

## Git 信息

- Wave base commit：
- 每个任务 base commit：
- 分支 / Worktree：
- 是否有未提交改动：
- 是否存在不应提交文件：

## 执行结果

| 任务编号 | 状态 | HEAD | 验证结果 | 交接包 |
| --- | --- | --- | --- | --- |
| TASK-001 | 待填写 | 待填写 | 待填写 | 待填写 |

## Review Hub 审查结果

- 待填写

## 用户动作等级汇总

| 任务编号 | UA 等级 | 是否需实机 | 用户动作 |
| --- | --- | --- | --- |
| TASK-001 | UA0 / UA1 / UA2 / UA3 / UA4 / UA5 / UA6 / UA7 | 是 / 否 / 待确认 | 待填写 |
```

## Parallel Wave 流程

1. 总览会话从 `TASK_BOARD` 中挑选候选任务。
2. 判断每个任务 A/B/C/D 等级。
3. 排除 D 级任务和高风险代码任务。
4. 检查任务依赖。
5. 检查文件锁 / 模块锁。
6. 检查 UA 等级，UA5 / UA6 / UA7 默认不得进入代码并行。
7. 创建或更新 `WAVE-xxx.md`。
8. 为每个并行任务指定执行会话。
9. 每个执行会话只执行自己负责的任务。
10. 每个任务记录自己的 base commit、HEAD、diff 范围、验证结果和交接包。
11. 执行完成后交给 Review Hub。
12. Review Hub 可以按 Wave 批量审查，但必须逐任务输出结论。
13. 如果某个任务出现 P0/P1、范围越界或冲突，单独拉出修复，不影响其他已通过任务。
14. 所有任务按各自 UA 等级进入验收建议。
15. 用户确认后再合并或关闭。

## 默认并行规模

建议默认：

- A 级文档任务：最多同时 5 个。
- B 级小代码任务：最多同时 2 个。
- C 级任务：最多 1 个代码任务，可并行 1~2 个 A 级文档任务。
- D 级任务：不和其他代码任务并行。
- 总执行会话数量建议不超过 3 个。

如果用户明确提高并行度，agent 仍必须先做冲突检查。

## Review Hub 审查要求

- 可以集中审查一个 Wave。
- 必须逐任务审查，不得只输出 Wave 总结论。
- 每个任务输出 P0/P1/P2/P3、UA 等级和是否允许进入验收建议。
- 每个任务结论写回对应 `TASK` 文件。
- 发现冲突、P0/P1 或 C/D 风险时，将该任务移出 Wave 单独处理。
