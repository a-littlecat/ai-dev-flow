# ai-dev-flow v0.8：AGENTS.md 最小兼容规则

> 把需要的条目合并进项目现有 `AGENTS.md`，不要整文件覆盖。项目自己的安全、构建和发布规则优先。

## 建议保留

```markdown
## AI 开发任务规则

- 先读用户要求、项目规则、Git 状态和直接相关文件，再修改。
- 低风险、单会话、最多 3 个业务文件、无高风险/真实环境/交付动作且确定性验证完整的任务，直接执行，不创建流程 TASK 或额外 Reviewer。
- 跨会话、范围较大或需要留证的任务使用 TASK；D 级、架构/依赖/数据/安全/真实环境/发布/不可逆动作使用最严格控制。
- TASK 是范围、状态和证据事实源；TASK_BOARD 只是索引和投影。
- 只修改当前任务允许范围，不顺手重构；范围、风险、依赖或 authority 改变时停止。
- 修改后运行最相关的构建、测试、静态检查或人工验证，并检查 diff。
- 审查只读，修复只处理明确 finding；缺少独立 Reviewer 时不得自批通过。
- 自动验证、Review、UA、Accepted、commit、merge、release、Closed 是不同状态，不得互相推导。
- 未经明确授权，不执行 merge、push、release、删除、外部同步或不可逆动作。
- 不输出或提交密钥、Token、账号密码、证书、隐私数据和本机敏感配置。
```

## 可选补充

项目确实需要时再增加：

- 构建、测试、lint 和运行命令；
- source of truth 与禁止修改目录；
- 分支/Worktree 规则；
- 真实环境证据和用户验收方式；
- 发布、数据迁移和安全审批人。

不要默认复制 Batch、Wave、Loop、Memory、Constitution、角色或 provider 规则。这些旧指南仍可按需读取，但不属于 v0.8 最小运行面。

## 冲突处理

- 项目 `AGENTS.md` 比本通用片段更具体时，保留项目规则。
- 规则冲突、权限不明或来源不明的工作区改动出现时，停止并说明最小阻塞信息。
- 不因采用本片段而批量迁移旧 TASK 或修改 `adf/v0.7.0` Contract。
